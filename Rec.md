# План улучшений DayView (на основе анализа DayZen)

## Текущее состояние проекта ✅
- Двойные 12-часовые часы (день/ночь)
- Markdown импорт/экспорт
- Категории с цветами
- Локализация EN/RU
- Темная/светлая тема
- Диалог редактирования задач

---

## 🎯 План реализации (по приоритету)

### Фаза 1: Визуальные улучшения (1-2 дня)

#### 1.1 True Black тема для OLED
**Файл:** `src/app/globals.css`
```css
.dark {
  --background: 0 0% 0%; /* Чистый черный вместо темно-синего */
}
```
**Сложность:** Низкая | **Влияние:** Высокое

#### 1.2 Неоновое свечение сегментов
**Файл:** `src/components/clock-diagram.tsx`
- Добавить CSS filter `drop-shadow` для сегментов
- Усилить свечение при hover
```tsx
<path 
  style={{ 
    filter: `drop-shadow(0 0 4px ${segment.color})`,
    opacity: hoveredSegment === segment.id ? 1 : 0.85 
  }}
/>
```
**Сложность:** Низкая | **Влияние:** Высокое

#### 1.3 Иконки/эмодзи в сегментах
**Файл:** `src/types/index.ts` — добавить поле `icon?: string`
**Файл:** `src/components/clock-diagram.tsx` — рендерить эмодзи в центре сегмента
**Сложность:** Средняя | **Влияние:** Среднее

---

### Фаза 2: Детекция конфликтов (2-3 дня)

#### 2.1 Функция проверки пересечений
**Новый файл:** `src/utils/overlap-detection.ts`
```typescript
export const detectOverlaps = (tasks: Task[]): Set<string> => {
  const overlapping = new Set<string>();
  for (let i = 0; i < tasks.length; i++) {
    for (let j = i + 1; j < tasks.length; j++) {
      if (timeRangesOverlap(tasks[i], tasks[j])) {
        overlapping.add(tasks[i].id);
        overlapping.add(tasks[j].id);
      }
    }
  }
  return overlapping;
};
```

#### 2.2 Визуальное предупреждение
**Файл:** `src/components/clock-diagram.tsx`
- Красная обводка для пересекающихся задач
- Пульсирующая анимация
- Красное свечение (`drop-shadow(0 0 8px #ef4444)`)

**Сложность:** Средняя | **Влияние:** Очень высокое (киллер-фича)

---

### Фаза 3: Статистика дня (2-3 дня)

#### 3.1 Компонент Reality Check
**Новый файл:** `src/components/day-stats.tsx`
- Прогресс-бар заполненности дня (% от 24ч)
- Цветовая индикация: зеленый (<80%), желтый (80-100%), красный (>100%)
- Количество конфликтов

#### 3.2 Статистика по категориям
- Время по каждой категории
- Визуализация через recharts (уже в зависимостях)

**Расположение:** Над списком задач или в отдельном табе

**Сложность:** Средняя | **Влияние:** Высокое

---

### Фаза 4: Улучшение UX (3-4 дня)

#### 4.1 Виджет текущей задачи
**Новый файл:** `src/components/current-task-widget.tsx`
- Показывает активную задачу
- Обратный отсчет до конца
- Следующая задача

#### 4.2 iOS-style Time Picker
**Файл:** `src/components/task-dialog.tsx`
- Заменить `<input type="time">` на scroll-picker
- Использовать `embla-carousel` или кастомный компонент

#### 4.3 Drag-and-drop на часах
**Файл:** `src/components/clock-diagram.tsx`
- Перетаскивание краёв сегментов для изменения времени
- Touch-события для мобильных

**Сложность:** Высокая | **Влияние:** Высокое

---

### Фаза 5: PWA и мобильная оптимизация (2-3 дня)

#### 5.1 PWA манифест
**Новый файл:** `public/manifest.json`
- Иконки для установки
- Offline-режим через Service Worker

#### 5.2 Адаптивный мобильный вид
- Один 24-часовой круг на мобильных
- Упрощенная навигация

---

## 📊 Сводка по файлам

| Файл | Изменения |
|------|-----------|
| `src/app/globals.css` | True black тема |
| `src/types/index.ts` | Добавить `icon` в Task |
| `src/components/clock-diagram.tsx` | Свечение, конфликты, drag |
| `src/utils/overlap-detection.ts` | НОВЫЙ: детекция пересечений |
| `src/components/day-stats.tsx` | НОВЫЙ: статистика |
| `src/components/current-task-widget.tsx` | НОВЫЙ: текущая задача |
| `src/components/task-dialog.tsx` | Time picker |

---

## 🚀 Рекомендуемый порядок

1. **Сначала:** Фаза 1 (визуал) — быстрая победа, сразу видно улучшение
2. **Затем:** Фаза 2 (конфликты) — главная фича DayZen
3. **Потом:** Фаза 3 (статистика) — добавляет ценность
4. **Далее:** Фаза 4 (UX) — полировка
5. **Наконец:** Фаза 5 (PWA) — расширение аудитории

---

## ❓ Что НЕ стоит копировать из DayZen

- **Ограничение на 3 задачи** — это вызвало негатив у пользователей
- **Только iOS** — веб-версия даёт преимущество
- **Один 24-часовой круг** — ваши два круга удобнее для чтения

---

*Готов начать с любой фазы. Какую реализуем первой?*
